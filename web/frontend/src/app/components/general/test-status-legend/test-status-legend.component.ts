import { Component, Input, ChangeDetectionStrategy } from '@angular/core';

@Component({
    selector: 'm-test-status-legend',
    templateUrl: 'test-status-legend.component.html',
    styleUrls: ['./test-status-legend.component.scss'],
    changeDetection: ChangeDetectionStrategy.OnPush,
})
export class TestStatusLegendComponent {
}
